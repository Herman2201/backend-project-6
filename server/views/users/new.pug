extends ../layouts/application.pug

block header
  = t('views.users.new.signUp')

block content
  +formFor()(action=route('users') method='post')
    +input(user, 'firstName', t('views.users.create.firstName'), errors)(type="text")
    +input(user, 'lastName',t('views.users.create.lastName'), errors)(type="text")
    +input(user, 'email', t('views.users.create.email'), errors)(type="text")
    +input(user, 'password',t('views.users.create.password'), errors)(type="password")
    +submit(user)(value=t('views.users.new.signUp'))
